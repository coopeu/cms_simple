<% @page_title = "Subjects" %>

<%= link_to("<< Back to Menu", {:controller => 'access', :action => 'index'}, :class => 'back-link') %>

<div class="subjects index">
  <h2>Temes</h2>

  <%= link_to(image_tag('plus_sign.png', :size => '11x11', :alt => '+') + " Add New Subject", {:action => 'new'}, :class => 'action new') %>

  <div><%= pluralize(@subjects.size, 'subject') %> found</div>
  <table class="listing" summary="Subject list">
    <tr class="header">
      <th>Order</th>
      <th>Subject</th>
      <th>Visible</th>
      <th>Pages</th>
      <th>Actions</th>
    </tr>
    <% @subjects.order('LOWER(position)').each do |subject| %>
    <tr class="<%= cycle('odd', 'even') %>">
      <td><%= subject.position %></td>
      <td><%= subject.name %></td>
      <td class="center"><%= status_tag(subject.visible) %></td>
      <td class="center"><%= subject.pages.size %></td>
      <td class="actions">
        <%= link_to("Mira Pàgines", {:controller => 'pages', :subject_id => subject.id}, :class => 'action show') %>
        <%= link_to("Mostra", {:action => 'show', :id => subject.id}, :class => 'action show') %>
        <%= link_to("Edita", {:action => 'edit', :id => subject.id}, :class => 'action edit') %>
        <%= link_to("Esborra", {:action => 'delete', :id => subject.id}, :class => 'action delete') %>
      </td>
    </tr>
    <% end %>
  </table>
</div>
